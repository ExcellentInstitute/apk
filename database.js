// database.js
// This file acts as the default blueprint for your database.
// If the browser has no saved memory, it will start with this empty structure.

const defaultDatabase = {
    students: [],
    transactions: [],
    stats: { 
        income: 0, 
        expense: 0, 
        balance: 0 
    }
};
